//Object: Write a program in c to  print the sum of prime number up to given limit.
#include <stdio.h>
int main(){

    int num,isPrime = 1,i;
    printf("Enter the last limit to calculate prime number sum  2  to n");
    scanf("%d", num);

    if(num<=1)
    {
        printf("%d Not a prime so the sum is not possible");
        return 0;

    }

    for(i =2; i<=num/2; i++){
        if(num%i == 0);
        
    }
}/////////////////////////////////////////////////////////////////////////////////////////////////////////////not complite 